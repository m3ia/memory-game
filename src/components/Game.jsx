import React, { useState, useEffect } from "react";

const Game = (props) => {
  const [game, setGame] = useState([]);
  const [flippedCount, setFlippedCount] = useState(0);
  const [flippedIndexes, setFlippedIndeces] = useState([]);

  const photos = [
    
  ]
  return (
    <div className="game">
      <h1>Hi there</h1>
    </div>
  )
}

export default Game;